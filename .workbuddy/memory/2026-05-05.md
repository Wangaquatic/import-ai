# 2026-05-05 工作日志

## 修复第一关（Level1Page）新手引导相关问题

### 问题
1. 行首孤儿代码：`const [showAllGuides, setShowAllGuides] = useState(false)` 被错误粘在 imports 之前（组件外部），导致运行时错误
2. 开头动画/教学引导"我明白了"关闭后再次进入关卡会重新出现
3. 教学引导模块弹出在二次进入关卡时会重置
4. 缺少美观的"查看全部引导"按钮
5. CSS 中有重复的大括号

### 第二次修改 - 重做第一关开头动画，仿照其他关卡

**需求：**
- 第一关要像第二、三、四关一样有开头动画，讲解分类器的作用
- 要有跳过按钮
- 每次进入都显示（与其他关卡一致）

**Level1Page.tsx 修改：**
1. `showTutorial` 从「持久化检查」改为 `useState(true)`（每次进入都显示，与其他关卡一致）
2. `handleCloseTutorial` 简化：只 `setShowTutorial(false)` + `setTutorialStep(0)`，不写 localStorage
3. 删除不再使用的 `LEVEL1_TUTORIAL_DONE_KEY` 常量
4. 重新设计教程动画步骤内容：
   - **步骤1**（0.5s）：粒子动画展示数据从输入端进入系统
   - **步骤2**（2.8s）：分类器对数据进行分析处理（分叉线 + 文字提示）
   - **步骤3**（4.8s）：展示分流结果「有目标 ↑」「无目标 ↓」
   - **步骤4**（6.8s）：总结页：分类器是二分类模型，可达75%准确率 +「我明白了」按钮
5. CSS 类名从 `level1-intro-*` 改为 `level1-tutorial-*`

**Level1Page.css 修改：**
1. 全部替换旧的 `.level1-intro-*` CSS（~210行）为新的 `.level1-tutorial-*` CSS
2. Skip 按钮样式与其他关卡统一（`bottom:30px;right:30px`）
3. 新关键帧动画：`tutorialParticleFlow`、`classifierGlow`、`splitPopIn` 等

### 修改内容

**Level1Page.tsx:**
1. 删除第1行的孤儿 `showAllGuides` 声明（位于 imports 之前）
2. 新增 `LEVEL1_TUTORIAL_DONE_KEY` 和 `LEVEL1_SEEN_MODULES_KEY` 常量
3. 修复 `showTutorial` 初始化逻辑：检查 `LEVEL1_TUTORIAL_DONE_KEY` 和 `LEVEL1_LEARNED_ALL_KEY` 两个持久化标记
4. 修复 `handleCloseTutorial`：改为设置 `LEVEL1_TUTORIAL_DONE_KEY`，确保永不清除
5. 删除冲突的 `useEffect`（原清除 `level1_intro_closed` 的逻辑）
6. 将 `seenModules` 改为从 `localStorage` 加载，删除每次挂载重置的 effect
7. 添加持久化 effect：`seenModules` 变化时自动保存到 localStorage
8. 在组件内添加 `showAllGuides` 状态
9. 美化"查看全部引导"按钮（改为 `show-all-guides-btn` className）

**Level1Page.css:**
1. 修复重复大括号（`.next-level-btn` 多了一个 `}`）
2. 添加 `.show-all-guides-btn` 按钮样式（固定在底部右侧，紫色风格）

### 第三次修改 - 修复主页按钮/第一关动画漂移

**主页（HomePage.css）：**
1. 底部导航按钮（商店、训练师档案）在 `:active` 时不再 `translateX(8px)` 偏移，改为仅 `scale(0.97)` 稳定缩放

**第一关教程动画（Level1Page.tsx + Level1Page.css）：**
1. 彻底重写布局：移除 content 容器的 `display:flex`，全部改用 `position:relative` + `position:absolute` 统一坐标锚定
2. 简化类名：从冗长的 `level1-tutorial-xxx` 改为短名如 `tut-classifier`、`tut-particles`、`tut-split`、`tut-summary`
3. 所有元素基于 720x500 容器的百分比/calc 坐标定位，消除 flex + absolute 混用导致的偏移
4. 粒子动画 `tutFly` 使用更简洁的 `left` 动画路径
